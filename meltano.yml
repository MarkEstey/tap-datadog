send_anonymous_usage_stats: true
project_id: tap-datadog
default_environment: test

environments:
- name: test

plugins:
  extractors:
  - name: tap-datadog
    namespace: tap_datadog
    pip_url: -e .
    repo: https://github.com/MarkEstey/tap-datadog
    maintenance_status: development
    capabilities:
    - state
    - catalog
    - discover
    - about
    - stream-maps
    settings:
    - { name: url_base, kind: string }
    - { name: api_key, kind: string, sensitive: true }
    - { name: application_key, kind: string, sensitive: true }
    - { name: start_date, kind: date_iso8601 }
    - { name: stream_type_conformance, kind: string }
    - { name: stream_maps, kind: object }
    - { name: stream_maps_config, kind: object }
    select:
    - '*.*'

  loaders:
  - name: target-jsonl
    variant: andyh1203
    pip_url: target-jsonl
    config:
      do_timestamp_file: true
